import type { HslaColor } from './color'

export interface ParticlesOptions {
  size?: number
  colors?: string | string[]
  length?: number
}

export interface BackgroundOptions {
  color?: HslaColor
}
